import 'package:flutter/material.dart';

class AudioBookItem extends StatelessWidget {
  const AudioBookItem({super.key});
  

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}